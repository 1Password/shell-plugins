version: "2"
linters:
  default: none
  enable:
    # Defaults:
    - errcheck
    - govet
    - ineffassign
    - unused
    # Extra:
    - asciicheck
    - bidichk
    - depguard
    - staticcheck

  settings:
    depguard:
      rules:
        main:
          deny:
            - pkg: math/rand
              desc: "please use the safer crypto/rand"

formatters:
  enable:
    - gofmt
    - goimports
